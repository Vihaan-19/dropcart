import { AppLayout } from "@/components/layout/app-layout";

export default function VendorsPage() {
  return (
    <AppLayout navItems={[]} title="Vendors">
      {/* TODO: VendorsTable, VendorFilters, and VendorActions components will go here */}
      <div className="text-lg text-muted-foreground">Vendors management coming soon.</div>
    </AppLayout>
  );
}
