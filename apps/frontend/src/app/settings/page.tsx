import { AppLayout } from "@/components/layout/app-layout";

export default function SettingsPage() {
  return (
    <AppLayout navItems={[]} title="Settings">
      {/* TODO: SettingsForm, ThemeSwitcher, and AccountSettings components will go here */}
      <div className="text-lg text-muted-foreground">Settings page coming soon.</div>
    </AppLayout>
  );
}
